import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  input: {
    flex: 1,
    fontSize: 20,
    color: '#333',
    width: '100%',
    borderColor: '#333',
    borderWidth: 1,
    borderRadius: 50,
    textAlign: 'center',
    marginBottom: 20,
    marginTop: 20,
  },
});

export default styles;
